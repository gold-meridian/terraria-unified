--- src/TerrariaNetCore/Terraria/GameContent/ExtraSpawnPointManager.cs
+++ src/Unified/Terraria/GameContent/ExtraSpawnPointManager.cs
@@ -221,7 +_,7 @@
 				return false;
 
 			if (settings.remix) {
-				if (WorldGen.GetWorldSize() == 0)
+				if (WorldGen.GetWorldSize() <= WorldSize.Small)
 					num2 = 150;
 
 				if (!settings.skyblock && (y < GenVars.remixSurfaceLayerLow || y > GenVars.remixMushroomLayerHigh))
@@ -231,7 +_,7 @@
 					return false;
 			}
 
-			if (settings.roundLandmass && WorldGen.GetWorldSize() == 0)
+			if (settings.roundLandmass && WorldGen.GetWorldSize() <= WorldSize.Small)
 				num2 = 150;
 
 			for (int i = 0; i < spawnPoints.Count; i++) {
