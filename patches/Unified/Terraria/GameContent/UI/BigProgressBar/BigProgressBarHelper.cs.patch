--- src/TerrariaNetCore/Terraria/GameContent/UI/BigProgressBar/BigProgressBarHelper.cs
+++ src/Unified/Terraria/GameContent/UI/BigProgressBar/BigProgressBarHelper.cs
@@ -1,3 +_,4 @@
+using System;
 using Microsoft.Xna.Framework;
 using Microsoft.Xna.Framework.Graphics;
 using ReLogic.Graphics;
@@ -30,7 +_,13 @@
 		int verticalFrames = 6;
 		Rectangle value2 = value.Frame(1, verticalFrames, 0, 3);
 		Color color = Color.White * 0.2f;
+
+		// TU: Make sure bosses with additional health don't overdraw.
+		/*
 		float num = lifeAmount / lifeMax;
+		*/
+		float num = Math.Clamp(lifeAmount / lifeMax, 0f, 1f);
+
 		int num2 = (int)((float)p.X * num);
 		num2 -= num2 % 2;
 		Rectangle value3 = value.Frame(1, verticalFrames, 0, 2);
