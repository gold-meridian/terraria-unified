--- src/TerrariaNetCore/Terraria/Initializers/LinkButtonsInitializer.cs
+++ src/Unified/Terraria/Initializers/LinkButtonsInitializer.cs
@@ -3,6 +_,7 @@
 using Microsoft.Xna.Framework.Graphics;
 using ReLogic.Content;
 using Terraria.DataStructures;
+using Terraria.Unified;
 
 namespace Terraria.Initializers;
 
@@ -19,13 +_,37 @@
 		titleLinks.Add(MakeSimpleButton("TitleLinks.Forums", "https://forums.terraria.org/index.php", 5));
 		titleLinks.Add(MakeSimpleButton("TitleLinks.Merch", "https://terraria.org/store", 6));
 		titleLinks.Add(MakeSimpleButton("TitleLinks.Wiki", "https://terraria.wiki.gg/", 7));
+
+		titleLinks = Main.UnifiedLinks;
+		titleLinks.Add(MakeSimpleButton("TitleLinks.Discord", "https://discord.gg/yog", 0));
+		titleLinks.Add(MakeSimpleButton("TitleLinks.Patreon", "https://patreon.com/c/steviegt6", 8));
+		titleLinks.Add(MakeSimpleButton("TitleLinks.KoFi", "https://ko-fi.com/steviegt6", 9));
 	}
 
 	private static TitleLinkButton MakeSimpleButton(string textKey, string linkUrl, int horizontalFrameIndex)
 	{
+		/*
 		Asset<Texture2D> asset = Main.Assets.Request<Texture2D>("Images/UI/TitleLinkButtons");
 		Rectangle value = asset.Frame(8, 2, horizontalFrameIndex);
 		Rectangle value2 = asset.Frame(8, 2, horizontalFrameIndex, 1);
+		*/
+
+		const int max_vanilla_frames = 8;
+		Asset<Texture2D> asset;
+		Rectangle value;
+		Rectangle value2;
+		if (horizontalFrameIndex < max_vanilla_frames) {
+			asset = Main.Assets.Request<Texture2D>("Images/UI/TitleLinkButtons");
+			value = asset.Frame(max_vanilla_frames, 2, horizontalFrameIndex);
+			value2 = asset.Frame(max_vanilla_frames, 2, horizontalFrameIndex, 1);
+		}
+		else {
+			horizontalFrameIndex -= max_vanilla_frames;
+			asset = ContentSources.ManifestAssets.Request<Texture2D>("Terraria.Unified.Assets.TitleLinkButtons", AssetRequestMode.ImmediateLoad);
+			value = asset.Frame(2, 2, horizontalFrameIndex);
+			value2 = asset.Frame(2, 2, horizontalFrameIndex, 1);
+		}
+
 		value.Width--;
 		value.Height--;
 		value2.Width--;
