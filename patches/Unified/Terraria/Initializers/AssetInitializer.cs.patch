--- src/TerrariaNetCore/Terraria/Initializers/AssetInitializer.cs
+++ src/Unified/Terraria/Initializers/AssetInitializer.cs
@@ -1,5 +_,7 @@
 using System;
+using System.Diagnostics;
 using System.IO;
+using System.Threading.Tasks;
 using Microsoft.Xna.Framework;
 using Microsoft.Xna.Framework.Graphics;
 using Newtonsoft.Json.Linq;
@@ -11,26 +_,61 @@
 using Terraria.GameContent;
 using Terraria.ID;
 using Terraria.IO;
-using Terraria.Testing;
 using Terraria.Utilities;
 
 namespace Terraria.Initializers;
 
 public static class AssetInitializer
 {
+	public class FxcReader(GraphicsDevice graphicsDevice) : IAssetReader
+	{
+		public async ValueTask<T> FromStream<T>(Stream stream, MainThreadCreationContext mainThreadCtx) where T : class
+		{
+			if (typeof(T) != typeof(Effect)) {
+				throw AssetLoadException.FromInvalidReader<FxcReader, T>();
+			}
+
+			var ms = new MemoryStream();
+			stream.CopyTo(ms);
+
+			await mainThreadCtx;
+			Debug.Assert(mainThreadCtx.IsCompleted);
+
+			return new Effect(graphicsDevice, ms.ToArray()) as T;
+		}
+	}
+
+	internal static AssetReaderCollection assetReaderCollection;
+
 	public static void CreateAssetServices(GameServiceContainer services)
 	{
-		AssetReaderCollection assetReaderCollection = new AssetReaderCollection();
+		assetReaderCollection = new AssetReaderCollection();
 		assetReaderCollection.RegisterReader(new PngReader(services.Get<IGraphicsDeviceService>().GraphicsDevice), ".png");
 		assetReaderCollection.RegisterReader(new XnbReader(services), ".xnb");
 #if XNA && !SERVER
 		assetReaderCollection.RegisterReader(new FxReader(services.Get<IGraphicsDeviceService>().GraphicsDevice), ".fx");
 #endif
+
+		assetReaderCollection.RegisterReader(new FxcReader(Main.instance.Services.Get<IGraphicsDeviceService>().GraphicsDevice), ".fxc");
+
+		/*
 		AsyncAssetLoader asyncAssetLoader = new AsyncAssetLoader(assetReaderCollection, 20);
 		asyncAssetLoader.RequireTypeCreationOnTransfer(typeof(Texture2D));
 		asyncAssetLoader.RequireTypeCreationOnTransfer(typeof(DynamicSpriteFont));
 		asyncAssetLoader.RequireTypeCreationOnTransfer(typeof(SpriteFont));
+		*/
+
+		XnbReader.LoadOnMainThread<Texture2D>.Value = true;
+		XnbReader.LoadOnMainThread<DynamicSpriteFont>.Value = true;
+		XnbReader.LoadOnMainThread<SpriteFont>.Value = true;
+		XnbReader.LoadOnMainThread<Effect>.Value = true;
+
+		AssetRepository.SetMainThread();
+
+		/*
 		IAssetRepository assetRepository = new AssetRepository(new AssetLoader(assetReaderCollection), asyncAssetLoader);
+		*/
+		var assetRepository = new AssetRepository(assetReaderCollection);
 		assetRepository.AssetValueUpdatedHandler = (AssetValueUpdated)Delegate.Combine(assetRepository.AssetValueUpdatedHandler, new AssetValueUpdated(TagAsset));
 		services.AddService(typeof(AssetReaderCollection), assetReaderCollection);
 		services.AddService(typeof(IAssetRepository), assetRepository);
