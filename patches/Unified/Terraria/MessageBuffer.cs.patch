--- src/TerrariaNetCore/Terraria/MessageBuffer.cs
+++ src/Unified/Terraria/MessageBuffer.cs
@@ -21,6 +_,7 @@
 using Terraria.Net.Sockets;
 using Terraria.Testing;
 using Terraria.UI;
+using Terraria.Unified.Features;
 
 namespace Terraria;
 
@@ -1128,7 +_,7 @@
 				BitsByte bitsByte5 = (byte)0;
 				BitsByte bitsByte6 = (byte)0;
 				BitsByte bitsByte7 = (byte)0;
-				Tile tile4 = null;
+				Tile tile4 = default;
 				for (int l = num62; l < num62 + num64; l++) {
 					for (int m = num63; m < num63 + num65; m++) {
 						if (Main.tile[l, m] == null)
@@ -1653,10 +_,19 @@
 				int num43 = reader.ReadByte();
 				string name = string.Empty;
 				if (num43 != 0) {
+					/*
 					if (num43 <= 20)
 						name = reader.ReadString();
 					else if (num43 != 255)
 						num43 = 0;
+					*/
+
+					if (num43 <= ChestNameLengthExtender.PreferredMaxChestNameLength) {
+						name = reader.ReadString();
+					}
+					else if (num43 != 255) {
+						num43 = 0;
+					}
 				}
 
 				if (Main.netMode == 1) {
@@ -2065,7 +_,7 @@
 				if (Main.tile[num14, num15] == null)
 					Main.tile[num14, num15] = new Tile();
 
-				lock (Main.tile[num14, num15]) {
+				// lock (Main.tile[num14, num15]) {
 					Main.tile[num14, num15].liquid = b3;
 					Main.tile[num14, num15].liquidType(liquidType);
 					if (Main.netMode == 2) {
@@ -2075,7 +_,7 @@
 					}
 
 					break;
-				}
+				// }
 			}
 			case 49:
 				if (Netplay.Connection.State == 6) {
